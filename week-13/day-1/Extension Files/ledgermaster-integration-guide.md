Integration guide — LedgermasterPage
====================================

Generated by Playwright Locator Inspector. Follow these steps to wire the
recorded flow into the Enterprise Playwright Automation Framework repo.

1. SAVE THE GENERATED FILES (required)
----------------------------------------
   - Save the "Page Object" tab's contents as src/pages/LedgermasterPage.ts
   - Save the "Spec" tab's contents as tests/e2e/ledgermaster.spec.ts
   - Nothing else has to change for this to run as-is: the spec
     constructs LedgermasterPage directly (new LedgermasterPage(page)) and only
     imports modules the framework already has (BasePage,
     SelfHealingLocator, @core/fixtures).

2. (OPTIONAL) REGISTER A FIXTURE FOR IDIOMATIC USAGE
----------------------------------------
   Every existing spec in this framework destructures its page objects as
   fixtures (e.g. { loginPage, dashboardPage }) instead of constructing
   them inline. To make LedgermasterPage consistent with that pattern, edit
   src/core/fixtures/pageFixtures.ts:

   --- src/core/fixtures/pageFixtures.ts --------------------------------
   + import { LedgermasterPage } from '@pages/LedgermasterPage';

     export interface PageFixtures {
       loginPage: LoginPage;
       dashboardPage: DashboardPage;
   +   ledgermasterPage: LedgermasterPage;
     }

     export const pageFixtures = base.extend<PageFixtures>({
       loginPage: async ({ page }, use) => { await use(new LoginPage(page)); },
       dashboardPage: async ({ page }, use) => { await use(new DashboardPage(page)); },
   +   ledgermasterPage: async ({ page }, use) => {
   +     await use(new LedgermasterPage(page));
   +   },
     });
   -----------------------------------------------------------------------

   src/core/fixtures/index.ts needs NO change — it already merges
   pageFixtures into the shared `test` export via mergeTests(...).

   If you do this, simplify the generated spec to use the fixture instead
   of constructing the page object inline:

     - async ({ page }) => {
     -   const ledgermaster = new LedgermasterPage(page);
     + async ({ ledgermasterPage }) => {
     +   const ledgermaster = ledgermasterPage;

   ...and drop the now-unused top-level import of LedgermasterPage.

3. MULTI-PAGE FLOW — DOUBLE-CHECK NAVIGATION
----------------------------------------
   This recording included 3 navigation step(s), captured as
   page.goto(<absolute URL>). Two things worth checking before this is
   final:

     - Prefer a relative path via the environment baseURL (see
       playwright.config.ts -> use.baseURL and the files under
       src/config/environments/) instead of the hardcoded absolute URL —
       the TODO comment in the spec flags exactly where.
     - If the navigation lands on a meaningfully different page/section,
       consider splitting this into a second Page Object rather than
       growing one class to cover both — this generator always produces a
       single Page Object per recording.

NO CHANGES NEEDED ELSEWHERE
----------------------------------------
   - src/core/locators/SelfHealingLocator.ts — the generated `strategies`
     arrays already use its exact builder signatures and tier order
     (primary -> secondary -> accessible); nothing to adjust.
   - src/core/base/BasePage.ts — the generated class only calls the
     protected helpers it already exposes (fill / click / resolve /
     textOf / isVisible).
   - playwright.config.ts — no project/config changes are required to run
     this spec, beyond whatever baseURL/environment you already have set up.

Registry: 4 distinct element(s), 7 step(s) total.